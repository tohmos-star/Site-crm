DATABASE_URL="postgresql://crm:crm@localhost:5432/crm?schema=public"
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
JWT_SECRET=change-me

# Только для docker-compose.yml (подставляется и в POSTGRES_PASSWORD, и в
# DATABASE_URL внутри контейнера app) — не используется самим приложением.
POSTGRES_PASSWORD=crm
